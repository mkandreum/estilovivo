# Production Environment Variables for Coolify
# These values should be configured in Coolify UI

# Database (provided by Coolify)
DB_USER=estilovivo
DB_PASSWORD=changeme
DB_NAME=estilovivo

# Server
NODE_ENV=production
PORT=3000
UPLOADS_DIR=/app/uploads

# Service URLs (provided automatically by Coolify)
SERVICE_URL_APP=https://estilovivo.xyoncloud.win
SERVICE_FQDN_APP=estilovivo.xyoncloud.win

# Authentication (CRITICAL - MUST be set)
JWT_SECRET=1713cd2057028b1dbbc5cd0b44b4106a0df1dcb66eb058763d09bf6de9ed733c

# Database URL (constructed from above vars)
DATABASE_URL=postgresql://estilovivo:changeme@db:5432/estilovivo?schema=public

# CORS (uses SERVICE_URL_APP)
CORS_ORIGIN=https://estilovivo.xyoncloud.win

# SMTP (optional - leave empty if not using email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

